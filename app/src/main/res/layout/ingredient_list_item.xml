<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/ingredient_name"
        android:textSize="@dimen/detail_main_text_size"
        android:textColor="@color/colorPrimary"
        android:id="@+id/ingredient_name"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/ingredient_measurement"
        android:contentDescription="@string/ingredient_measurement"
        app:layout_constraintLeft_toRightOf="@+id/ingredient_name"
        android:textSize="@dimen/detail_main_text_size"
        android:paddingStart="@dimen/ingredient_list_paddings"
        android:paddingLeft="@dimen/ingredient_list_paddings"
        android:paddingRight="@dimen/ingredient_list_paddings"
        android:paddingEnd="@dimen/ingredient_list_paddings"
        />

</android.support.constraint.ConstraintLayout>